# Meridian

Executive dashboards generated from Excel workbooks. Each project links one workbook to one dashboard with its own theme and share link.

- `npm run dev` — local development (copy `.env.example` to `.env.local`)
- `npm run build` — production build
- `/api/health` — readiness JSON

See `CLAUDE.md` for the decision log.
